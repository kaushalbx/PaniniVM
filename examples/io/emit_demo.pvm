# I/O Stream & Emitting Operations Demo in PaniniVM Script (.pvm)
# Emits output stream payload using hu (हावय/हु)

# Step 1: Emit output stream payload
हविस् + अम् हु + णिच् + लोट् + सिप् ।

# Step 2: Print emitted result confirmation
मुद्र् + णिच् + लोट् + सिप् फल + अम् ।
