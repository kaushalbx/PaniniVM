# Scaling / Growth Algorithm in PaniniVM Script (.pvm)
# Scales input quantity using edh (एधय)

# Step 1: Scale input quantity 5
पञ्च + अम् एध् + णिच् + लोट् + सिप् ।

# Step 2: Print scaled result
मुद्र् + णिच् + लोट् + सिप् फल + अम् ।
